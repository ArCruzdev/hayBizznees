/* eslint-disable react/prop-types */
const Cabeceras = ({ titulo }) => {
  return (
    <nav className="cabecera">
    <div className="container-cabecera">
        <h2>{titulo}</h2>
    </div>
    </nav>
  )
}

export default Cabeceras
